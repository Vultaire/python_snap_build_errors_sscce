[build-system]
requires = ["hatchling >= 1.21"]
build-backend = "hatchling.build"

[project]
name = "sscce"
version = "0.1.dev1"
authors = [
    {name="Paul Goins", email="paul.goins@canonical.com"},
]
description = "A snap build test using Python"
requires-python = ">=3.12"   # Ubuntu Noble's python3 package

[project.scripts]
sscce = "sscce.main:main"
